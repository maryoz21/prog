{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Arrancar servidor",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": [
                "backend.routes:app",
                "--reload",
                "--port", "5000"
            ],
            "jinja": true,
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}
